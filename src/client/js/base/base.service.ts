// import factory from "../factory/factory";

export default class BaseService {
    static token;
    static isSingleton;
    static factoryFn;

    static addProvider() {
        // factory[this.token] = this.factoryFn;
    }

}